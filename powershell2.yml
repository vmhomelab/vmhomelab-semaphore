---
- hosts: windows
  become: false
  gather_facts: false

  tasks:

  - name: Extract PS output.
    shell: 
      cmd: "C:\\somescript.ps1"
    register: pmon

  - name: Display PS output.
    debug:
      msg: "{{ pmon.stdout_lines }}"


